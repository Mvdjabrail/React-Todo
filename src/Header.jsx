import React from "react";
import css from '../src/header.module.css'

function Header(props) {
    return(
        <div className={css.header}>
        Список дел
      </div>
    )
}

export default Header
